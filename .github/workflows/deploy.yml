name: Deploy

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.event.workflow_run.conclusion == 'success'

    environment:
      name: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - name: Package artifact
        run: zip -r publish.zip . -x "*.git*"

      - name: Upload publish artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-publish
          path: publish.zip

      # -------------------------------------------------------
      # Option A: Deploy to Azure App Service
      # Uncomment and add AZURE_WEBAPP_NAME + AZURE_PUBLISH_PROFILE in repo secrets
      # -------------------------------------------------------
      # - name: Deploy to Azure App Service
      #   uses: azure/webapps-deploy@v3
      #   with:
      #     app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
      #     publish-profile: ${{ secrets.AZURE_PUBLISH_PROFILE }}
      #     package: publish.zip

      # -------------------------------------------------------
      # Option B: Deploy to server via SSH (IIS / Linux self-hosted)
      # Add SSH_HOST, SSH_USER, SSH_PRIVATE_KEY in repo secrets
      # -------------------------------------------------------
      # - name: Copy files to server via SCP
      #   uses: appleboy/scp-action@v0.1.7
      #   with:
      #     host: ${{ secrets.SSH_HOST }}
      #     username: ${{ secrets.SSH_USER }}
      #     key: ${{ secrets.SSH_PRIVATE_KEY }}
      #     source: "publish.zip"
      #     target: "/var/www/myapp"
      #
      # - name: Restart app on server
      #   uses: appleboy/ssh-action@v1.0.3
      #   with:
      #     host: ${{ secrets.SSH_HOST }}
      #     username: ${{ secrets.SSH_USER }}
      #     key: ${{ secrets.SSH_PRIVATE_KEY }}
      #     script: |
      #       cd /var/www/myapp
      #       unzip -o publish.zip
      #       systemctl restart myapp

      # -------------------------------------------------------
      # Option C: Deploy to AWS Elastic Beanstalk
      # Add AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, EB_APP_NAME, EB_ENV_NAME in secrets
      # -------------------------------------------------------
      # - name: Deploy to AWS Elastic Beanstalk
      #   uses: einaregilsson/beanstalk-deploy@v22
      #   with:
      #     aws_access_key: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     aws_secret_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     application_name: ${{ secrets.EB_APP_NAME }}
      #     environment_name: ${{ secrets.EB_ENV_NAME }}
      #     region: us-east-1
      #     version_label: ${{ github.sha }}
      #     deployment_package: publish.zip

      # -------------------------------------------------------
      # Option D: Deploy to Docker / Kubernetes
      # Add DOCKER_USERNAME, DOCKER_PASSWORD in repo secrets
      # -------------------------------------------------------
      # - name: Log in to Docker Hub
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}
      #
      # - name: Build and push Docker image
      #   uses: docker/build-push-action@v5
      #   with:
      #     context: .
      #     push: true
      #     tags: ${{ secrets.DOCKER_USERNAME }}/myapp:${{ github.sha }}
